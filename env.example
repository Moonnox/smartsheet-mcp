# Remote Server Configuration
# Copy this file to .env and update with your values

# Server Configuration
PORT=8080
HOST=0.0.0.0

# Authentication
REQUIRE_AUTH=true
SECRET_KEY=your-secret-key-here-change-this

# Debug Mode (set to true for verbose logging)
DEBUG=false

# Note: Smartsheet API credentials are passed per-request via headers:
# - x-smartsheet-api-key: Your Smartsheet API token
# - x-smartsheet-endpoint: (optional) Smartsheet API endpoint
# - x-allow-delete-tools: (optional) Enable delete operations

